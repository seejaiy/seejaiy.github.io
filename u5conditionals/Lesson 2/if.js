let age = 20;
let quote = '"mentally stable"';
if (age >= 18) {
    alert("You are old enough to vote, so register now! (assuming you are a U.S. citizen, have a clean record, and fit the government's standard of " + quote + ")");
}
